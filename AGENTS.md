# AGENTS.md — 二开工作副本

本检出目录是 [getpaseo/paseo](https://github.com/getpaseo/paseo) 的**下游二开仓库**，不是干净的上游源码树。

- **二开产品线：** [finelly977/paseo](https://github.com/finelly977/paseo) — `origin`
- **原作者上游：** [getpaseo/paseo](https://github.com/getpaseo/paseo) — `upstream`（只允许拉取，已禁用推送）

仓库所有者使用此仓库进行独立二次开发，**不再跟随、合并或挑选原作者代码**。私有产品改动只在二开仓库演进；上游仅作为新功能与共性缺陷的只读情报来源。

产品架构、包结构、编码规范和关键运行约束仍以 [CLAUDE.md](CLAUDE.md) 与 `docs/` 为准。本文只规定**仓库身份、二开功能清单维护方式和上游情报评估策略**。

## 二开功能清单（关键）

[docs/fork-features.md](docs/fork-features.md) 是当前二开功能的唯一清单。任何会改变二开产品行为的任务，都必须在同一次交付中同步维护该文件。

该文件不是更新日志，严禁按时间顺序不断追加历史记录。维护规则如下：

1. **新增二开功能：** 新增一条当前有效功能说明，写清用户行为、适用边界和与上游的差异。
2. **修改已有二开功能：** 直接改写原条目，使其准确描述修改后的最终行为，不要在后面追加“本次又修改了什么”。
3. **移除二开功能：** 删除对应条目；如果只是部分撤销，则改写为仍然有效的剩余行为。
4. **上游能力重合：** 如果上游后来也具备某项能力，应根据双方当前实际行为重新判断差异；行为完全一致时从清单删除，只保留二开仍然额外提供的部分。
5. **交付前审计：** 对照代码实际行为检查清单，禁止保留已经失效、被覆盖或与代码不一致的描述。

评估上游新能力或共性缺陷时必须优先对照此清单，确认二开是否已经覆盖以及独立实现时不得退化的现有行为。

## 远端配置（预期状态）

| 远端       | 地址                                      | 用途                             |
| ---------- | ----------------------------------------- | -------------------------------- |
| `origin`   | `https://github.com/finelly977/paseo.git` | 所有本地工作的默认拉取和推送目标 |
| `upstream` | `https://github.com/getpaseo/paseo.git`   | 原作者发布版本的只读来源         |

禁止把 `origin` 改回原作者仓库，禁止向 `upstream` 推送。

## 上游情报评估策略（关键）

当用户要求检查、评审、同步、合并或拉取原作者最新版时，例如“作者最新版”“同步上游”“看最新更新”或“merge upstream”，默认含义都是**检查上游情报并报告**，不把上游代码接入本仓库。除非用户之后明确撤销“不再跟随上游”的长期决定，否则不得执行代码合并。

### 1. 默认范围只看最新已发布版本

- 使用上游最新的 GitHub Release 或 Git 标签，包括预发布版和 beta，例如 `v0.2.0-beta.4`。
- 优先执行 `gh release list --repo getpaseo/paseo` 和/或 `git fetch upstream --tags`，按发布时间确定最新已发布标签；除非用户明确要求稳定版，否则稳定版和 beta 一起比较。
- 情报分析以该标签或标签指向的提交为基准，不能默认使用浮动分支头。

### 2. 未经明确要求不得使用未发布代码

- 不得把 `upstream/main`、开放中的 PR 分支或无标签提交当作默认“最新版”。
- 日常评估不包含原作者尚未完成或尚未发布的工作。
- 只有用户明确提到 `main`、“未发布”“HEAD”、具体分支名或 PR 编号时，才允许检查对应的未发布内容。

### 3. 只评估两类有价值的信息

- **新功能：** 上游新增、当前二开没有且对用户确实有价值的能力。报告用户收益、适用场景和二开是否已有等价能力，不分析“能否直接挑选提交”。
- **共性缺陷：** 上游修复的缺陷可能也存在于二开。必须检查二开当前代码、测试或可复现路径后再下结论；不能只因文件名或功能相似就声称二开也有该问题。
- 纯重构、依赖升级、品牌内容、上游专属界面调整和二开已经解决的问题默认不列为候选；但安全、数据损坏、构建失败等高影响问题仍需报告。
- 评估阶段只报告，不自动修改二开代码。用户明确选择某项能力或缺陷后，再按本仓库当前架构独立设计、实现和验证。

### 4. 禁止接入上游代码

- 禁止执行 `git merge`、`git rebase`、`git cherry-pick` 上游提交，禁止应用上游补丁、整文件覆盖或通过其他方式把上游代码历史接入二开。
- `git fetch upstream --tags`、查看提交和比较行为仍然允许，但只能用于研究。
- 用户选择落地某项能力或修复后，必须以二开现有代码为基础独立实现，并保留 [docs/fork-features.md](docs/fork-features.md) 中的现有行为；不得把“参考上游”变成隐蔽的补丁移植。
- `origin` 始终保持为二开仓库，任何二开提交只能推送到 `origin`。

### 5. 情报记录（关键）

[upstream-merges/](upstream-merges/) 仅保留过去已经完成的上游合并历史，现已冻结，不得继续把它当作待合并清单或新增合并记录。

- 今后的上游评估记录统一放在 [upstream-reviews/](upstream-reviews/)；一个已评估发布标签对应一个文件，记录新功能候选、经确认的共性缺陷以及排除项。
- 记录必须区分“上游修复了某问题”和“二开经检查也存在该问题”，不得把未经验证的相似性写成二开缺陷。
- [docs/fork-features.md](docs/fork-features.md) 继续只记录当前产品行为，不记录上游评估流水账。

### 已发布版本的默认检查命令

```powershell
git fetch upstream --tags
gh release list --repo getpaseo/paseo --limit 10
# 确定最新发布标签（包括 beta）后，例如：
git log --oneline <上次已评估标签>..vX.Y.Z
git diff --stat <上次已评估标签>..vX.Y.Z
```

### 仅在用户明确要求时检查未发布内容

```powershell
git fetch upstream
git log --oneline <比较基准>..upstream/main
# 只用于研究和报告，不合并或挑选提交
```

## 日常开发

- 本地产品开发默认使用 `main`，跟踪 `origin/main`。
- 功能开发使用二开仓库上的短期分支，例如 `feat/…`、`fix/…`，完成后通过 PR 或合并进入二开 `main`。
- 日常开发分支不得建立在 `upstream` 的提交、标签或分支上；上游远端只用于只读研究。

### 提交与推送

- 同一次交付中完成至少 3 个独立需求时，完成验证后必须创建本地 Git 提交，不再等待用户另行要求提交。
- 自动提交只能包含本次任务相关文件；工作区存在无关改动时必须排除，禁止混入同一提交。
- 推送仍必须等待用户明确要求。未经明确指示，不得因为已经自动提交就自行推送。

## 本文件不代表什么

- 不能替代 [CLAUDE.md](CLAUDE.md) 中的产品和工程规则。
- 不代表可以发布原作者项目的 npm 包或 GitHub Release。
- 不要求持续跟踪原作者 `main`。
