# v0.7.0 合并记录

| 项目         | 值                                                            |
| ------------ | ------------------------------------------------------------- |
| 上游发布日期 | 2026-08-31                                                    |
| 上游基线     | `v0.6.1`                                                      |
| 上游提交数   | 95（正式版后续增量 38 个，其中实质改动 32 个、发布噪音 6 个） |
| 分诊日期     | 2026-08-29 起                                                 |
| 合并状态     | 正式版分诊完成；已合并一部分，其余按下文选择性适配            |

本文件把正式版发布前两批已经完成的分诊、合并和丢弃结论合并到 `v0.7.0` 正式版名下。原有结论和二开落地状态全部保留；从第二批结束提交 `4e60c2880` 到正式标签 `v0.7.0` 的 38 个提交也已经逐项分诊。分诊完成不代表代码已经合并，状态仍以各条目的“建议”和后续回填为准。

## 已完成分诊的第一批

本轮最重的一个标签。三件需要你拍板:**许可证改成 Apache-2.0**、**全新插件体系**、**副本缓存重构**。另有若干可直接合的修复。

### 先读:这个标签里有重复提交

上游 `d3b10f029`(把 `next` 分支合进 `main`)让四个 PR 的两条血缘同时进了历史,**同一个 PR 在本标签内出现两次,且内容并不相同**(经补丁校验,只有 #2337 两侧一致):

| PR                 | 两个提交                  | 两侧是否一致 |
| ------------------ | ------------------------- | ------------ |
| #3642 回退长对话   | `c3ae54a1a` / `e19d3914a` | 否           |
| #3612 匀速输出     | `dac6cd141` / `e22ad117f` | 否           |
| #2337 托管平台检查 | `b79b4bd5f` / `4a5716360` | 是           |
| #2501 F-Droid      | `43d27328e` / `f6288629f` | 否           |

**不要逐个 cherry-pick 这四组。** 应直接取标签边界的净差异:

```powershell
git diff v0.6.1..v0.7.0 -- <相关路径>
```

否则会先应用旧版本、再应用新版本,产生难以排查的冲突。

### 需要你决策

#### 1. `a8734a972` 许可证从 AGPL-3.0 改为 Apache-2.0

**做什么**
上游把 `LICENSE` 和所有 `package.json` 的 license 字段从 `AGPL-3.0-or-later` 换成 `Apache-2.0`,同步改了四种语言的 README 和两篇文档。

**对二开的影响**
Apache-2.0 比 AGPL 宽松,取消了"提供网络服务也必须开源"的义务。二开目前继承 AGPL。跟不跟随是**法律决定,执行者不得自行选择**。

**必须注意:这个提交是混装的。** 71 个文件里只有约 10 个和许可证有关,其余是完全不相干的功能改动:

- 桌面主进程重构(`main.ts` 减 248 行)和新增的窗口归属模块
- 新增浏览器截图功能、"用外部程序打开"逻辑重写
- 桌面守护进程管理器改动——正好压在二开功能 17(预启动内置守护进程)上
- 应用侧的加密垫片、项目工具、侧栏固定钩子
- CLI 的 `agent ls` 命令

**结论：已按用户决策切换为 Apache-2.0。** 只整合许可证正文、包元数据和产品文档中的许可证声明；没有引入该混装提交里的桌面窗口、守护进程管理器、浏览器截图和侧栏改动。

#### 2. 插件体系（8 个提交，新增 `packages/plugin` 工作区及公共扩展能力）

**做什么**
一整套第三方扩展机制:插件可以从 Git 仓库直接安装和更新、自定义时间线条目的显示方式、使用宿主提供的图标和主题色、读取已注册项目列表。附带新的 CLI 命令、示例插件和文档。

涉及提交:`f82886f28`、`8e0af11b6`、`7ae5133ed`、`ecfb77b01`、`12e3acd18`、`4de913fb8`、`4c46f2017`、`657152c47`,以及 `v0.7.0` 的 `deb846eed`、`fe803f6e7`。

**对二开的影响**
**缺前置。** 二开完全没有插件体系(上游 0.5.0 引入),这整条线是从零引入。主要风险点是 `7ae5133ed` 要求对话视图开放"让插件改写时间线条目"的接口——那正是二开功能 5、6、11 重度改造的文件。

**结论：已按用户决策整组引入。**

- 不打算支持第三方插件 → **整体丢弃**是干净的选择,代价是与上游对话视图的结构分歧继续扩大。
- 打算支持 → **整组一次性引入**,不要零散挑选,否则会得到一个残缺的扩展接口。

其中 `657152c47`(SDK 暴露已注册项目列表)是唯一能脱离插件体系单独合并的,风险低。

#### 3. 副本缓存改行存储(`b12daabe3` 等 5 个提交)

**做什么**
把应用本地那份"离线副本缓存"(用来在连上主机之前先把上次的对话画出来)从整块存储改成按行存储,目的是让长对话的重启恢复更快、写盘更省。涉及 `b12daabe3`、`579c6a58f`、`f59db03d6`、`fd22784e2`、`0f68e55ec`。

**对二开的影响**
**冲突面最大的一项。** `b12daabe3` 一次改 232 个文件(它同时是一个混装的压缩提交,里面还夹带了 Hub 引导设置等无关功能),覆盖对话视图的间距、布局、虚拟化、网页渲染策略全家桶,以及会话状态仓库和应用根布局——正好是二开功能 5(对话排版间距)、6(历史索引与消息跳转)、11(成功回合折叠)、13(常驻界面渲染)的全部核心。它修改的 150 个文件里有 46 个在二开不存在。

**关键风险:** 二开功能 6 明确规定"应用重启后从本地缓存副本恢复的会话必须按配置重新权威加载,缓存尾部不得当作已同步的完整历史,也不得用它的陈旧光标走增量路径"。上游这次重构改的正是这份缓存的存储粒度。合并前必须先确认这条不变量在新结构下怎么表达,否则会静默退回旧缺陷——表现为打开老会话时历史加载不全,而且不报错。

**建议:待决策,优先级排在许可证和插件之后。** 收益是性能,代价是重做二开对话视图的四条功能,不值得先动。

### 建议合并

#### `59e623278` 权限请求被取消后,权限卡片一直卡在那里

**做什么**
提供方有时会在用户还没点选之前就取消一个权限请求。这时 Paseo 收不到结束事件,界面上那张"是否允许"的卡片就永远停在等待状态。合并后为这种情况补发一个明确的结束事件。

**对二开的影响**
两个文件在二开都存在,可直接应用。方向和二开功能 16 一致——二开的提交审查浮窗同样依赖权限结束事件才能收尾。

**建议:合并。**

#### `96cf0f2ae` 升级 Claude Agent SDK 到 0.3.246

**做什么**
把 Claude Agent SDK 从 0.3.220 升到 0.3.246,保持精确版本锁定。上游说明称类型接口只增不减。

**对二开的影响**
**高,必须审计。** 二开功能 12 依赖 SDK 的后台任务变更信号来判断"Claude 还有后台子代理在跑",从而避免空闲回收把 CLI 进程杀掉。SDK 升级后如果这个信号的语义或名称变了,功能 12 会**静默失效**——表现为下次打开会话时又出现"上一个 Claude 进程退出时后台智能体仍在运行"的失败通知。

另外它修改的 `claude/options.ts` 在二开不存在(二开的 Claude 适配器文件布局不同),需要手工对应。

**建议:合并,但升级后必须实测一次后台子代理场景。** 二开有 `agent.background-tasks.test.ts`,先跑它。

#### `2d7896999` + `5c7d62df8` 让 ACP 类智能体也支持运行中追加消息

**做什么**
智能体正在跑的时候直接追加一句话(而不是打断重来)这个能力,扩展到声明支持它的 ACP 提供方。`5c7d62df8` 是上游随后对 ACP 回合处理的一次部分回退。

**对二开的影响**
二开已经挑入了完整的运行中追加链路,这是把同一能力延伸到 ACP。四个文件在二开都存在,可直接应用。

**建议:两条一起合并,注意先后顺序。** 只合前一条会停在上游已判定有问题的中间状态。

#### `96cb70f5c` iOS 上回退对话后输入框是空的

**做什么**
回退到某条历史消息时,应该把那条消息的文字恢复到输入框里。iOS 上文字和"恢复"指令被拆成两次送达,输入框在重新挂载之前一直是空的。合并后把文字和指令一起送。

**对二开的影响**
方向和二开功能 4 一致(二开要求回退时恢复文本和图片)。可直接应用。

**建议:合并。**

#### `3377997f3` 在首选编辑器里直接打开子文件夹

**做什么**
文件浏览器里可以对子文件夹选择"用外部编辑器打开",并且不可用的目标不再显示在菜单里。

**对二开的影响**
它修改的文件在二开基本都存在,缺的只有韩语语言包和一个桌面端测试。和二开功能 8(Windows PowerShell 打开方式)、功能 16(在文件管理器中定位)同属"打开方式"这条线,需要确认菜单项不重复、PowerShell 目标仍在。

**建议:合并,合并后检查一遍"打开方式"菜单。**

#### `4e52125f5` 浅色主题下选中的工作区没有高亮

**做什么**
Light 主题里选中的工作区看不出被选中。

**对二开的影响**
改的是主题文件,而二开功能 5 只改了默认 Dark 主题的层级配色,两者不重叠,预期只有文本冲突。

**建议:合并。**

#### `b6d18d0c0` 旧版桌面端会删掉新版写入的设置

**做什么**
和 `v0.5.2` 的设置问题同源,但发生在桌面端自己的 `desktop-settings.json`:旧版本读到不认识的键就丢弃。合并后在文档、设置、嵌套设置和迁移四个层级都保留未知键。

**对二开的影响**
二开功能 17 在桌面设置里加了"退出时是否停止守护进程"的默认值差异,正是会被旧版丢掉的那类键。

**建议:合并。**

#### `d46267aed` 手动检查到的桌面更新会被自动检查清掉

**做什么**
你手动点"检查更新"发现了一个新版本,随后的自动就绪轮询又把灰度门槛重新套一遍,把这个已经放行的更新清掉了。合并后同版本的放行状态保持单向,不会被自动检查撤销。

**对二开的影响**
二开功能 9 把更新源隔离到 `finelly977/paseo`。合并后必须确认更新检查仍然只看二开仓库,不会去拉上游的安装包。

**建议:合并,合并后验证更新源。**

#### `cda441d3b` 从设置页返回后输入框高度闪一下

**做什么**
设置页显示时,后面保留挂载的工作区面板宽度会被压到零,这个无效尺寸被当成真实测量值写进去,回到对话页时输入框高度先错后对,肉眼可见闪一下。合并后把零宽度当成"面板不可见",不参与测量。

**对二开的影响**
和二开功能 13(设置页切换时保留侧栏挂载状态)是同一类问题——二开也保留隐藏面板的挂载。可直接应用。

**建议:合并。** 与 `v0.5.1` 的两条输入框高度修复同源,一起处理更省事。

#### `b0ce40804` ACP 智能体报告"终端能力不可用"

**做什么**
ACP 类智能体默认没有声明终端能力,导致它们以为不能用终端。合并后默认开启。

**对二开的影响**
三个文件,无二开交集。

**建议:合并。**

#### `da930b4c7` 西班牙语"关闭"相关文案翻译错误

**做什么**
单文件翻译修正。

**对二开的影响**
二开功能 16 要求新增界面文案在全部语言资源中保持同构,方向一致。

**建议:合并。**

### 需要先审计

#### `e22ad117f` / `dac6cd141` 助手文字一顿一顿地往外蹦

**做什么**
智能体输出的文字是按 60 毫秒一批送过来的,每批里的字数随模型输出速度剧烈波动,而界面收到多少就画多少——看起来就是一卡一卡地往外蹦。合并后改成匀速渲染,视觉上像稳定打字。

**对二开的影响**
高。改对话视图和消息组件,是二开功能 5、11 的核心文件。好消息是它修改的 11 个文件在二开**全部存在**,可直接应用。

**建议:合并,但要盯住二开功能 11 的"回合成功后收起中间过程"。** 匀速渲染改变了文字到达的节奏,确认收起时机不会被推迟。按上文"重复提交"提醒取净差异。

#### `e19d3914a` / `c3ae54a1a` 回退长对话时整个历史重新加载一遍

**做什么**
回退对话后,守护进程会重建时间线。旧做法是把重建后的历史整个重放一遍,长对话看起来就是"从头重新加载"。合并后有能力的客户端改成只刷新已有的尾部,老客户端仍走重放。

**对二开的影响**
高。改智能体管理器、提示词处理、客户端能力声明和回退组件,正好压在二开功能 4(Codex 与 Claude 使用不同的原生回退策略)上。缺失的三个文件都是测试。

**建议:合并,必须逐条对照功能 4 审计。** 二开的 Codex 是原地回退、Claude 是分支出新会话,两种路径下"时间线被替换"的信号语义不同,别让新的尾部刷新路径把其中一种漏掉。按上文取净差异。

#### `0ad69d3eb` 重启后要等半天才看到对话内容

**做什么**
冷启动时,应用会先把所有保存过的主机全部恢复一遍,再发布任何会话。于是一份本地已经缓存好、完全可以立刻画出来的对话,被卡在无关的目录校验和网络就绪后面。合并后缓存内容按需直接绘制,网络回来后再权威对账。

**对二开的影响**
高。51 个文件,涉及会话状态仓库、会话上下文、客户端活动心跳、工作区界面和侧栏模型,覆盖二开功能 6、7、13。而且它**依赖副本缓存行存储重构**(缺 `row-store-sqlite.ts`),属于上文第 3 项决策的下游。

同样要盯二开功能 6 的那条不变量:缓存先画出来没问题,但不能因此认为历史已经加载完整。

**建议:等副本缓存决策之后再处理。** 单独合并会缺前置。

### 不跟随(右侧面板策略)

按 [README 的右侧面板策略](../README.md#右侧面板不跟随上游结构),下列条目不合并。

#### `46b401eac` 快捷键和"打开更改"的去向调整

**做什么**
`Cmd/Ctrl+E` 改成只切换 Explorer 显示与否,不再顺便切换视图;输入框上方的"更改"按钮和拉取请求的打开位置改由统一的路由决定;最大化按钮是否可用改为根据当前可见面板判断。

**为什么不跟随**
整条改动围绕 Explorer 的显示与打开位置。修改的 20 个文件里有 8 个在二开不存在(Explorer 侧栏模块、布局设置页)。

**结论:丢弃。** 其中"最大化按钮按可见面板判断"是个通用的小改进,如果二开也有同样的错误行为,自己改比摘补丁快。

#### `5a38a3334` 手机上"更改"和拉取请求开在了错误的位置

**做什么**
Explorer 改造后,手机等紧凑布局下点"更改"或拉取请求会开成普通工作区标签,而不是进 Explorer。合并后紧凑布局统一进 Explorer。

**为什么不跟随**
修的是上游 Explorer 改造自己引入的回归,二开没有 Explorer 也就没有这个回归。

**结论:丢弃。**

#### `2755596b9` 更改列表和滚动差异的文件顺序对不上

**做什么**
"更改"视图里同一批文件有两种顺序:左边的树把目录排在文件前面,右边滚动的差异按完整路径逐字节比较。于是像 `.gitlab-ci.yml` 这种根目录文件,点它能打开差异,但它在树里排在所有目录下面——滚动差异时和从上往下看树对不起来。

**为什么不跟随**
改的是上游自己的更改列表和差异排序,二开功能 16 是另一套实现,补丁打不上也不解决二开的问题。

**结论:丢弃,但值得自查。** 二开的更改列表和差异正文如果也是两套排序,会有同样的错位。花两分钟在一个根目录有文件的仓库里对一下顺序即可确认。

#### `b79b4bd5f` / `4a5716360` 托管平台检查状态更细(75 文件)

**做什么**
给 GitLab 和 Gitea 的 CI 检查增加"需要手动触发""需要你操作""警告"这几种状态,并修正 Gitea 把单个作业当成整条流水线、GitLab 丢失作业语义的问题。

**为什么不跟随结构**
拉取请求面板属于二开重构范围,且它依赖上游 0.5.0 的检查结果汇总(缺 `check-group-state.ts`、`checks-ring.tsx`)。

**结论:结构不跟随。** 但"区分手动触发 / 需要操作 / 警告"是个真实的新能力——只有你实际用 GitLab 或 Gitea 时才值得在二开自己的面板上做一个;用 GitHub 的话直接丢弃。

### 已丢弃

#### `43d27328e` / `f6288629f` F-Droid 发布元数据

**做什么**
为 F-Droid(Android 开源应用商店)生成发布元数据和变更日志。

**丢弃原因**
二开不发布 F-Droid 渠道,这套元数据没有使用场景。若将来开始发布 Android 开源渠道,再单独回补。

### 已合并

- `a8734a972`：仅整合 Apache-2.0 许可证正文和相关声明；提交中混入的其他产品代码未合并。
- `4de913fb8`：插件守护进程会话固定使用当前守护进程版本，能够读取完整提供方目录。
- `ecfb77b01`：支持从 Git 仓库安装、检查和原子更新插件；候选版本必须先校验、编译并启动成功才切换，失败时继续使用原版本。
- `4c46f2017`：补齐客户端运行时、模块边界和宿主能力文档。
- `657152c47`：公共 SDK 可以读取已注册项目列表。
- `f82886f28`：插件界面使用语义主题变量，并与二开主题、字体和字号设置同步。
- `7ae5133ed`：插件可以转换和渲染投影后的时间线条目；二开保留自己的完整历史索引、虚拟化和成功回合收起逻辑。
- `8e0af11b6`：插件可以使用宿主校验并渲染的 Lucide 图标。
- `12e3acd18`：恢复原生平台的插件初始化和管理流程。

### 冲突已解决

- 时间线扩展点接入二开现有投影与本地副本重投影流程，不修改服务端规范时间线，也不绕过二开的历史窗口和内存预算。
- 插件工作区面板统一进入二开正文标签系统；声明为 Explorer 的打开位置仍映射为正文标签，不引入上游右侧栏结构。
- 插件主题先按上游语义色构建，再叠加二开黑白 Dark、界面字体、字号和语法高亮设置。

## 已完成分诊的第二批

分诊时上游的最新已发布版本。修复密度高、结构性改动少,是本轮性价比最高的一批。

### 建议合并

#### `a1ed5a8ba` 拉取请求轮询把你整个 GitHub 账号的配额耗光

**做什么**
拉取请求状态轮询按工作区各自计时,每个工作区一次查询。工作区超过 50 个左右时,就会把 GitHub 每小时 5000 点的 API 配额**整台机器耗尽**——不只是 Paseo 用不了,你在这台机器上其他用 GitHub API 的工具也会一起被限流。合并后改成全局调度:所有到期目标凑到同一个 5 秒栅格上,一次批量查询最多 25 个目标。

**对二开的影响**
三个文件在二开都存在,可直接应用。二开功能 16 的托管平台状态行正是这个轮询的消费方。

**建议:优先合并。** 这是本批里影响外溢到 Paseo 之外的唯一一条。

#### `fb67d2cdc` Git 排队时守护进程假死、客户端掉线

**做什么**
Git 进程的启动是同步补充的,机器上 Git 启动慢的时候会把主循环占死,占到连接保活检测超时——表现为客户端莫名其妙断线重连。合并后改成每轮事件循环只放行一个进程,空闲时第一条命令会立即执行,不给正常的顺序 Git 操作增加延迟。

**对二开的影响**
中高。改工作区 Git 服务和 Git 命令层,是二开功能 16 的服务端支撑。二开自己做过工作区 Git 刷新优化(`cd99f4f58`,改成首次调度后忽略后续事件),需要确认两套节流不会叠加成"半天不刷新"。

**缺前置:** 它依赖上游的 Git 进程调度器模块,二开没有这个文件,需要连同引入。

**建议:合并,合并后实测一次外部改文件后 Git 状态多久刷新。**

#### `23debf3af` 通过 Paseo 提交会绕过你配置的 Git 签名

**做什么**
Paseo 在应用内提交和 squash 合并时强制关掉了签名,你在 Git 里配置的签名策略不生效。合并后交给 Git 按用户配置执行。

**对二开的影响**
二开功能 16 提供了完整的提交流程,这条直接作用在那上面。两个文件都存在,可直接应用。

**建议:合并。** 如果你用签名提交,这是个静默产生未签名提交的缺陷。

#### `25eb322ef` 归档掉的工作区又从缓存里冒出来

**做什么**
本地缓存的读取和"目录已被替换"的写入会抢跑,把已经在内存里删掉的记录又读回来——你归档了一个工作区,它过一会儿又出现在侧栏。合并后读取排在待写入之后,主机切换时丢弃在途读取,本地归档在确认删除前不做缓存恢复。

**对二开的影响**
二开功能 1 依赖"重新进入工作区时自动打开最近活跃且未归档的根智能体",归档项复活会干扰这个选择。缺失的两个文件是测试。

**建议:合并。**

#### `7ea428811` 恢复的工作区标签陷入死循环,焦点被拽走

**做什么**
启动时重复的标签会把工作区焦点挪到 Explorer;之后清理逻辑删掉过期智能体,空工作区的对账又不断往里塞草稿并重新触发自己,形成循环。合并后在对账边界保留普通面板的焦点。

**对二开的影响**
两个文件都存在,可直接应用。但要注意:它的修复方式涉及 Explorer 焦点归一化,二开没有 Explorer,需确认这部分改动在二开是空操作而不是引入新概念。

**建议:合并,注意 Explorer 相关代码在二开是否有对应物。**

#### `42b6d99d6` 语音听写结尾的一段话丢了

**做什么**
停止录音时,原生录音已经先关掉了,最后一段麦克风缓冲还没送过来,你如果立刻点发送就会丢掉结尾。合并后等最后一段音频落地再完成停止,并把启动、停止、销毁串成一条顺序链。

**对二开的影响**
无二开交集,可直接应用。

**建议:合并。**

#### `51a435d3c` 用 PR 编号找工作区

**做什么**
命令面板原本只能按标题、主机、项目、分支找工作区,你记得的那个 PR 编号反而搜不到。合并后 `42`、`#42`、`!42`、`pr 42`、`mr 42` 都能直接定位,且按编号精确匹配(输 42 不会带出 142)。

**对二开的影响**
六个文件在二开都存在,可直接应用。二开有命令面板本体。

**建议:合并。** 这条不像同批其他命令面板改动那样缺前置。

#### `a501e1e05` SDK 能读到会话自己加载了哪些斜杠命令

**做什么**
守护进程一直能查询运行中会话加载了哪些斜杠命令和技能,但 SDK 的智能体句柄没暴露出来,导致 SDK 使用方只能看到磁盘上扫得到的,看不到智能体自带的内置命令。合并后补上这个方法。

**对二开的影响**
纯增量接口,无冲突。

**建议:合并。**

### 需要先审计

#### `3d69b0f45` Markdown 把 (c) 显示成 ©、把引号变成弯引号

**做什么**
Markdown 渲染器开了"排版美化"选项,会在解析后替换字符:`(c)` 变成 `©`、`...` 变成省略号、`---` 变成破折号、直引号变成弯引号。智能体经常用 (a)(b)(c) 标记选项,渲染出来就成了 (a)(b)©。更麻烦的是,智能体输出的文本你会直接复制到终端或文件里,弯引号在屏幕上看着对,粘贴过去就报错。文件预览也有同样的问题,显示的字符和磁盘上的不一致。合并后原样渲染。

**对二开的影响**
它修改的 `assistant-markdown-parser.ts` 在二开不存在——二开功能 11 自己做了一套共享的 Markdown 解析(`markdown-ast.ts`),文件布局不同。

**建议:值得要,但需要手工移植。** 改动本身很小(关掉一个选项),移植到二开的解析器和 `markdown-styles.ts` 上即可,不要照搬文件。

#### `ed628ff82` 把工作区操作加进命令面板

**做什么**
八个原本只能从侧栏菜单、工作区头部菜单或死记快捷键才能用的操作——重命名工作区、复制工作区路径、复制分支名、固定/取消固定、三个布局开关、显示设置向导——全部进命令面板,另外新增标签分配。同时把重命名对话框从侧栏行组件里挪出来,否则那一行没渲染时(项目折叠、分组折叠)对话框会跟着消失。

**对二开的影响**
**缺前置。** 修改的 11 个文件里有 5 个在二开不存在,是上游 0.5.0 引入的命令面板工作区贡献点。同时压在二开功能 14(行菜单关闭后悬停残留)、18(侧栏组织方式)、1 上。

**建议:待定。** 想要的话得先补命令面板的工作区贡献点。重命名对话框那部分修复本身有价值,可以单独手工移植。

### 需要你决策

#### `667664847` 通过 SSH 连接远程守护进程

**做什么**
桌面端和 CLI 可以用你现有的 OpenSSH 配置连到远程机器上的守护进程,是继本地直连和加密中继之后的第三条路。只做连接,远程安装和守护进程的启停仍是单独的操作。

**对二开的影响**
34 个文件里只缺韩语语言包,主体可应用。改到桌面守护进程管理器,二开功能 17 在那里加了预启动逻辑。

**建议:待决策。** 功能自足、质量看起来不错,但会引入一套新的连接状态机和凭据处理面。只在本机用桌面端的话可以延后;有远程主机场景的话,这条比配中继省事得多。

#### 插件体系续集

`deb846eed`(插件可以用 Paseo 的弹窗、图标和提示条)、`fe803f6e7`(插件可以在输入框上方贡献上下文小标签)。

**结论：随 `v0.7.0` 的插件体系整组引入，并完成二开布局适配。**

### 已合并

- `deb846eed`：插件可以使用 Paseo 宿主弹窗、图标和提示条；运行时上下文在弹窗内部保持可用。
- `fe803f6e7`：插件可以注册客户端入口和输入框上下文标签；标签会在插件卸载、禁用、替换、宿主断线或应用卸载时释放。
- `a501e1e05`：公共 SDK 的智能体句柄可以读取完整会话快照和斜杠命令目录，供插件和其他 SDK 调用方使用。
- `ed628ff82`：先补齐缺失的工作区贡献点，再把工作区操作接入命令面板；侧栏专有悬停和紧凑层级继续由二开组件负责。
- `fb67d2cdc`：在已补齐的 Git 进程调度器上整合压力保活核心，前台命令优先，守护进程不会因后台 Git 队列拥塞失去响应。

### 冲突已解决

- `fe803f6e7`：没有恢复二开已经删除的输入框轨道和智能体轨道文件。插件标签接入现有输入框上方区域，无标签时不占布局；插件面板仍进入正文标签。
- `ed628ff82`：只引入命令面板的工作区贡献与操作能力，不用上游侧栏行组件覆盖二开的会话排序、紧凑间距、悬停修复和菜单行为。
- `fb67d2cdc`：只整合服务端调度与运行指标，不修改二开 Git 面板、文件列表、提交图表和右侧栏交互。

### 已丢弃

_(尚无)_

## 正式版后续增量

- 范围：`4e60c2880..v0.7.0`，共 38 个提交。其中 32 个包含实际产品或工程改动，6 个只是版本号、更新日志或锁文件变化。
- 状态：已完成逐项分诊，尚未把下列“建议适配”的能力写入二开代码。
- 约束：后续只以 `v0.7.0` 正式标签为目标，不恢复 beta 标签或台账；右侧 Git 面板界面继续由二开独立维护，上游提交即使没有文本冲突也不能覆盖二开行为。

### 建议优先适配的独立修复

#### `3c6e4e39e`：支持 Astro 文件高亮

- **做什么：** 让 Astro 源文件在代码块、差异内容和服务端差异高亮中按正确语法着色，不再退化成普通文本。
- **对二开的影响：** 改动集中在高亮包，和二开面板布局、会话时间线没有行为冲突；需要连同 Astro 解析器许可证和测试一起引入。
- **建议：** **适配合并。** 功能独立、用户收益明确，按当前高亮包结构移植即可。

#### `463415ae8`、`74a377ff6`：增强 Pi、OMP 和 JSONL 提供方进程可靠性

- **做什么：** Pi 和 OMP 启动 RPC 时允许最多等待 60 秒，并在超时信息中指出卡在哪个启动阶段；守护进程向已经关闭的 JSONL 标准输入写入时，不会因 `EPIPE` 直接退出。
- **对二开的影响：** 两个提交修改同一套 JSONL 进程基础设施，应作为一个可靠性修复组适配。二开必须继续把超时、进程退出和管道断开作为明确失败返回，不能只记录日志后假装启动成功。
- **建议：** **优先适配合并。** 同时保留原始错误原因、启动阶段和退出状态，并补 Pi、OMP、管道断开三类定向测试。

#### `a88aa49f4`：修正 Grok 统一计费额度展示

- **做什么：** Grok 使用统一计费接口时，把额度解释为每周使用窗口，而不是按错误的周期展示。
- **对二开的影响：** 只影响额度查询与展示，不改变二开已有的 Grok 会话清理、进程命名和生命周期行为。
- **建议：** **适配合并。** 改动独立且能直接消除错误额度信息。

#### `9113542c5`：正确结束不会再到达最终片段的语音输入

- **做什么：** 语音识别只留下临时片段、随后不再产生最终片段时，也能结束录音并保留可用结果，不会一直卡在等待状态。
- **对二开的影响：** 服务端语音流管理器中的独立正确性修复，与二开界面无冲突。
- **建议：** **适配合并。** 必须保留“真正失败”和“临时片段已成为最终可用结果”的状态区别。

#### `5e561f8a2`：语音输入结束后释放 Android 通信音频通道

- **做什么：** Android 完成语音输入后释放蓝牙或通话音频路由，避免后续媒体声音仍走低质量通信通道。
- **对二开的影响：** 仅涉及 Android 原生音频模块；桌面端不受影响。
- **建议：** **适配合并。** 二开仍发布 Android 端，应保留此修复并执行原生模块定向检查。

#### `11e76f665`：失败回合后可以正常继续发送

- **做什么：** 上一轮启动失败或执行失败后，新消息不再被旧错误立即拒绝；服务端会明确记录新一轮处于等待、已启动或启动失败哪个阶段。
- **对二开的影响：** 与二开近期修改过的运行中追加、Codex 回退、运行时恢复和错误状态代码高度重叠，不能直接挑选提交。处理不当会让新消息重复执行，或把旧失败错误地显示为新一轮失败。
- **建议：** **优先适配但需深度审计。** 用二开现有运行状态模型实现，并覆盖“旧轮失败后新轮成功”“新轮本身仍启动失败”“追加消息与轮次恰好结束并发”三类测试。

#### `169482827`：显示 OpenCode 子会话的用户要求

- **做什么：** OpenCode 子智能体会话即使没有完成时间，也能显示它收到的用户要求，不再只看见后续过程或空白记录。
- **对二开的影响：** 修改范围集中在 OpenCode 历史转换，需保留二开完整文件编辑列表、回合边界和运行中追加标记。
- **建议：** **适配合并。** 属于历史完整性修复，完成后用有完成时间和无完成时间的子会话各验证一次。

### 建议成组整合的能力

#### `aeb240be9`：批量查询冷启动时的拉取请求状态

- **做什么：** 服务端首次识别多个仓库后，把拉取请求状态查询合并调度，避免同时向 GitHub 发出大量重复请求。
- **对二开的影响：** 依赖较早但尚未合并的 `a1ed5a8ba` 全局轮询批处理基础。当前二开的 GitHub 服务没有该调度结构，单独应用本提交既不完整也无法得到同样效果。它属于服务端支撑，不受“Git 面板界面不跟随上游”限制。
- **建议：** **与 `a1ed5a8ba` 成组适配。** 不单独挑选；整合时验证多个工作区冷启动、请求失败和速率限制不会被误报成“没有拉取请求”。

#### `6c813d1a9`、`8cf5942a5`、`8533c5c06`：保持输入框、键盘和焦点同步

- **做什么：** 手机端键盘升降时输入框与自动补全位置保持同步，展开后的输入框不会被键盘遮挡；网页和桌面端发送消息后继续聚焦输入框，可直接输入下一条。
- **对二开的影响：** 三个提交共同修改输入框停靠、输入法适配和智能体面板。二开另有紧凑输入框、缩小发送/停止按钮、上下文环和可调间距，直接覆盖会造成布局回退。
- **建议：** **成组适配。** 只移植键盘同步和焦点行为，保留二开尺寸、间距和按钮样式；原生补丁、网页焦点与展开输入框场景要一起验证。

#### `1d4c3a272`：插件可以请求宿主打开智能体、路由和工作区面板

- **做什么：** 插件界面能通过受控接口导航到指定智能体、应用页面或工作区工具面板，插件模板和公共契约也会暴露这些能力。
- **对二开的影响：** 二开已经合并插件体系，但文件与工具面板采用正文标签，不采用上游 Side/Explorer 结构。直接应用会把插件导航接到不存在或已废弃的面板宿主。
- **建议：** **适配合并。** 保留公共插件能力，把所有面板导航映射到二开的正文标签和现有路由；不得恢复上游右侧栏或改动二开 Git 面板。

#### `650f3ca7f`：普通 OpenCode 会话复用共享辅助进程

- **做什么：** 多个普通 OpenCode 会话共享已经启动的服务和桥接插件，减少重复 Node/OpenCode 进程、内存占用与首次发送等待时间。
- **对二开的影响：** 收益很高，但改动跨越提供方注册、运行时、快照、守护进程启动和打包脚本。二开已有提供方预热、手动释放运行时、按会话释放、OpenCode 自动批准与权限选择，整合错误会造成跨会话环境泄漏、误释放或权限串用。
- **建议：** **作为独立架构组适配。** 先明确共享进程和会话私有状态边界，再验证打包、恢复、释放单个会话、并发权限请求和不同工作区环境隔离，不做零散挑选。

#### `eef9be886`、`86ef46800`、`261868667`：可缩放图片预览

- **做什么：** 对话图片、文件图片和 Mermaid 预览支持放大、缩小、拖动、双击缩放与稳定关闭；后两个提交补齐 Android 手势和快速操作时的画面稳定性。
- **对二开的影响：** 三个提交共享同一缩放视图组件，必须一起处理。二开已经修复 Codex 历史图片和本地附件恢复，替换消息图片渲染时不能丢失这些来源。
- **建议：** **成组适配。** 同时覆盖历史图片、本地附件、文件预览、Mermaid、Android 手势和桌面关闭交互。

#### `63587a9c0`、`b1050d9be`、`adf80a9fe`：会话权限与 Hub 访问边界

- **做什么：** 服务端按操作语义授权会话；Hub 登录和导出入口更简单；Hub 守护进程只获得明确授予的能力，而不是依赖连接来源进行模糊判断。
- **对二开的影响：** 三个提交跨协议、客户端、服务端、终端和 Hub，后两个依赖第一个的权限模型。直接挑选其中一个会让旧客户端连接失败或给 Hub 过多权限。二开还必须保留本机可信客户端、现有智能体权限下拉和明确的失败提示。
- **建议：** **成组审计后适配。** 如果继续保留 Hub，就完整迁移权限契约和兼容握手；权限不足必须返回可区分失败，不能当成资源不存在或空数据。

### 需要局部提取或等待前置工作的改动

#### `b6bfdedf5`：归档会话导航、缓存恢复与 OpenCode 活动状态修复

- **做什么：** 打开归档会话时先准备正确工作区标签；恢复客户端副本缓存时串行读写，避免导航到尚未准备好的会话；OpenCode 用明确活动标记判断会话是否仍在运行。
- **对二开的影响：** 缓存部分依赖二开此前暂缓的副本行存储重构，并直接触及二开已经修过的后台同步、旧会话折叠、归档导航和重新打开后实时更新。整体挑选有较高复发风险。
- **建议：** **不直接合并。** 先单独提取归档导航与 OpenCode 活动判断；缓存串行化等副本存储方案确定后再处理，并用后台完成、归档后重新打开和历史分页场景验证。

#### `6688c92bc`：不再把智能体首次工作误称为“重新打开”

- **做什么：** 新任务开始时使用“开始工作”一类正确文案，只有真正恢复旧任务时才显示“重新打开”。
- **对二开的影响：** 展示和状态判断有价值，但提交还夹带多语言资源与副本缓存修改。韩语资源在当前仓库的上游合并规则中明确不跟随，缓存部分又依赖尚未决定的存储重构。
- **建议：** **局部适配。** 只提取任务状态语义、简体中文和当前已维护语言的文案；暂不引入韩语文件和依赖新副本行存储的代码。

#### `ce335de1c`：让 Paseo 自有工具调用更易读

- **做什么：** 把内部工具调用从原始参数展示改成有标题、有状态和有针对性详情的可读卡片，让用户知道工具正在做什么以及结果如何。
- **对二开的影响：** 协议层的详情结构值得引入，但界面修改落在二开高度定制的消息渲染、已完成过程折叠、工具汇总和文件路径展示上，直接替换会破坏现有排版或再次漏掉多个编辑文件。
- **建议：** **分层适配。** 先合并协议解析与测试，再在二开工具卡片上接入这些详情，保留完整文件列表、折叠边界、字号和可调间距。

#### `0dad1d7a6`：改善 iPad 宽屏模型选择器滚动

- **做什么：** iPad 使用桌面式宽布局时，模型选择器改用适合弹出层的滚动方式，长模型列表可以正常浏览。
- **对二开的影响：** 修改面较小，但二开模型选择器和提供方预热行为已有调整，不能覆盖当前下拉加载逻辑。
- **建议：** **按 iPad 场景适配。** 只引入滚动容器选择，不改变模型预热、筛选和选择状态。

#### `4a3e6dea6`：提高移动端正文默认字号

- **做什么：** 新用户的移动端对话正文默认使用 16 像素，应用框架文字保持 15 像素；一次性迁移只调整仍使用旧默认值的用户。
- **对二开的影响：** 二开已经提供统一的界面字号和对话字号设置，并修复过悬停才更新、同一区域字号不一致等问题。直接采用上游设置键和迁移会覆盖用户明确选择，或再次产生两套字号来源。
- **建议：** **按二开设置模型改写后再合并。** 迁移只作用于从未自定义正文大小的移动端用户，任何显式设置都必须保持不变。

#### `63923fdd0`：点击 Android 时间线文字时避免列表跳动

- **做什么：** Android 倒序时间线里点击可选择文字时不再触发列表自动调整，阅读位置保持稳定。
- **对二开的影响：** 真正有价值的是 React Native 补丁和补丁安装脚本；同一提交还修改了上游拉取请求面板测试与检查列表，这部分属于二开明确不跟随的 Git 面板界面。
- **建议：** **只适配 Android 原生补丁。** 丢弃拉取请求面板相关改动，并确认补丁与当前 React Native 版本完全匹配后再登记。

### 已丢弃

#### `693280d99`：上游 Git 提交列表避开 Android 安全区域

- **做什么：** 调整上游 Git 提交列表底部留白，避免内容被 Android 系统区域遮住。
- **对二开的影响：** 只修改上游 `commits-section` 界面；二开 Git 面板是独立实现，布局和交互不再跟随上游。
- **建议：** **丢弃。** 若二开 Android Git 面板出现同类问题，应在二开组件中独立修复，不能恢复上游结构。

#### `7080648e1`：发布上游网站的条款与隐私政策

- **做什么：** 为原作者网站增加服务条款、隐私政策和页脚入口。
- **对二开的影响：** 文案声明的运营主体、服务边界和数据处理责任属于原作者，不能直接作为二开产品的法律文本发布。
- **建议：** **丢弃。** 二开需要法律页面时必须按自己的主体和实际数据流单独编写。

#### `375e9b3e5`、`fa53ba8f9`、`73a5e6c63`、`f2f93087e`：上游官网宣传内容

- **做什么：** 重做原作者官网产品示意图、响应式展示、首页故事和竞品对比文案。
- **对二开的影响：** 内容服务于原作者品牌，并展示上游面板布局；合并既会覆盖二开品牌，也会错误宣传二开已经不同的 Git 和文件面板。
- **建议：** **丢弃。** 不把上游营销站点和产品截图当作应用功能更新。

#### `b0fe4efc8`、`fcd81f591`、`95f4eb757`、`f35d86072`、`185ab3f30`、`c56638ea8`：上游发布记录与版本噪音

- **做什么：** 写入 beta 与正式版更新日志、切换上游包版本、更新 Nix 依赖哈希和 Android 商店发布说明。
- **对二开的影响：** 不包含需要移植的产品逻辑；直接合并会把二开的包版本、发布记录和商店元数据改成原作者版本。
- **建议：** **丢弃。** 二开版本、更新日志、依赖哈希和发布元数据继续按自身发布流程维护。

### 后续整合顺序

1. 先处理 JSONL/Pi/OMP 可靠性、Grok 额度、失败回合恢复、OpenCode 子会话、语音收尾和 Android 音频释放这些独立修复。
2. 再分别处理 OpenCode 共享辅助进程、插件导航、图片缩放、键盘同步、拉取请求轮询、工具调用展示和 Hub 权限等成组能力；每组单独验证，不把多条高风险功能压成一次不可审查的合并。
3. 归档导航与副本缓存只提取已能证明安全的行为；Git 面板界面、上游官网和发布版本文件保持丢弃。
